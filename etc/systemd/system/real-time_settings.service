[Unit]
Description=enable real-time settings for audio applications

[Service]
Type=oneshot
ExecStart=/opt/admintools/real-time_settings.py -i Cron on
ExecStop=/opt/admintools/real-time_settings.py -i Cron off
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
